'use strict';

/**
 * uac-achievement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::uac-achievement.uac-achievement');
