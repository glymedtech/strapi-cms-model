'use strict';

/**
 * days-of-glymed controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::days-of-glymed.days-of-glymed');
